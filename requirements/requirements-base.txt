# =============================================================================
# Base Requirements - Shared dependencies for both MLX and GPU environments
# =============================================================================

# Data Processing
datasets>=2.19.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# Tokenization
tokenizers>=0.15.0
sentencepiece>=0.2.0

# Evaluation Metrics
sacrebleu>=2.4.0
evaluate>=0.4.0
rouge-score>=0.1.2

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.0.0

# Experiment Tracking
wandb>=0.16.0
mlflow>=2.10.0
tensorboard>=2.15.0

# Hugging Face Hub
huggingface-hub>=0.21.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Formatting / Linting
black>=24.0.0
ruff>=0.3.0

# Jupyter (for notebooks)
jupyterlab>=4.1.0
ipywidgets>=8.1.0

# Korean NLP (for Use Case 1)
# Note: konlpy requires Java - install separately if needed
kiwipiepy>=0.16.0
