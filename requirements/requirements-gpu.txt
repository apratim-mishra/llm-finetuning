# =============================================================================
# GPU Requirements - Cloud NVIDIA GPU Training Environment
# Install: pip install -r requirements-gpu.txt
# Note: Requires CUDA 12.1+ installed on system
# =============================================================================

-r requirements-base.txt

# PyTorch with CUDA 12.1
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0

# Transformers Ecosystem
transformers>=4.45.0
accelerate>=0.28.0
peft>=0.10.0
trl>=0.12.0

# Quantization (4-bit, 8-bit)
bitsandbytes>=0.43.0

# Flash Attention 2 (requires Ampere+ GPU: A100, H100, RTX 3090+)
# Note: May need to install from source on some systems
# flash-attn>=2.5.0

# Training Optimizations
# liger-kernel>=0.2.0  # For Llama-family speedup
# unsloth>=2024.4      # 2x LoRA speedup (optional)

# Distributed Training
deepspeed>=0.14.0

# Evaluation
lm-eval>=0.4.0

# Inference (production serving)
vllm>=0.6.0

# Additional
safetensors>=0.4.0
scipy>=1.11.0

# COMET for translation evaluation (optional - large model download)
# unbabel-comet>=2.2.0
